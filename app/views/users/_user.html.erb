<li>
  <%= gravatar_for user, :size => 30 %>
  <%= link_to user.name, user %>
  <% if current_user.admin? %>
  <%= link_to 'delete this user', user, :method => :delete, :confirm => 
              'Jestes pewien?', :title => "usuwanie #{user.name}" %>
  <% end %>
</li>
